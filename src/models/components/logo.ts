/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type Logo = {
  type?: "Logo" | undefined;
  /**
   * The text visible inside the logo (OCR)
   */
  content?: string | undefined;
  /**
   * A detailed description of the logo
   */
  description: string;
  base64Data?: string | null | undefined;
};

/** @internal */
export const Logo$inboundSchema: z.ZodType<Logo, z.ZodTypeDef, unknown> = z
  .object({
    type: z.literal("Logo").default("Logo"),
    content: z.string().default(""),
    description: z.string(),
    base64_data: z.nullable(z.string()).optional(),
  }).transform((v) => {
    return remap$(v, {
      "base64_data": "base64Data",
    });
  });
/** @internal */
export type Logo$Outbound = {
  type: "Logo";
  content: string;
  description: string;
  base64_data?: string | null | undefined;
};

/** @internal */
export const Logo$outboundSchema: z.ZodType<Logo$Outbound, z.ZodTypeDef, Logo> =
  z.object({
    type: z.literal("Logo").default("Logo" as const),
    content: z.string().default(""),
    description: z.string(),
    base64Data: z.nullable(z.string()).optional(),
  }).transform((v) => {
    return remap$(v, {
      base64Data: "base64_data",
    });
  });

export function logoToJSON(logo: Logo): string {
  return JSON.stringify(Logo$outboundSchema.parse(logo));
}
export function logoFromJSON(
  jsonString: string,
): SafeParseResult<Logo, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Logo$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Logo' from JSON`,
  );
}
