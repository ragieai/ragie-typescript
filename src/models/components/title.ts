/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type Title = {
  type?: "Title" | undefined;
  /**
   * The text content
   */
  content: string;
};

/** @internal */
export const Title$inboundSchema: z.ZodType<Title, z.ZodTypeDef, unknown> = z
  .object({
    type: z.literal("Title").default("Title"),
    content: z.string(),
  });
/** @internal */
export type Title$Outbound = {
  type: "Title";
  content: string;
};

/** @internal */
export const Title$outboundSchema: z.ZodType<
  Title$Outbound,
  z.ZodTypeDef,
  Title
> = z.object({
  type: z.literal("Title").default("Title" as const),
  content: z.string(),
});

export function titleToJSON(title: Title): string {
  return JSON.stringify(Title$outboundSchema.parse(title));
}
export function titleFromJSON(
  jsonString: string,
): SafeParseResult<Title, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Title$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Title' from JSON`,
  );
}
