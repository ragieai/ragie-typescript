/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type Signature = {
  type?: "Signature" | undefined;
  /**
   * Content of the signature.
   */
  content: string;
  /**
   * A detailed description of the signature.
   */
  description: string;
  /**
   * The printed text indicating who should sign (e.g., 'Driver Signature', 'Authorized By').
   */
  label: string;
  /**
   * True if a handwritten signature, digital signature, or stamp is present. False if blank.
   */
  isSigned: boolean;
  /**
   * The name of the signer
   */
  signerName?: string | null | undefined;
  /**
   * The date of the signature if present
   */
  date?: string | null | undefined;
};

/** @internal */
export const Signature$inboundSchema: z.ZodType<
  Signature,
  z.ZodTypeDef,
  unknown
> = z.object({
  type: z.literal("Signature").default("Signature"),
  content: z.string(),
  description: z.string(),
  label: z.string(),
  is_signed: z.boolean(),
  signer_name: z.nullable(z.string()).optional(),
  date: z.nullable(z.string()).optional(),
}).transform((v) => {
  return remap$(v, {
    "is_signed": "isSigned",
    "signer_name": "signerName",
  });
});
/** @internal */
export type Signature$Outbound = {
  type: "Signature";
  content: string;
  description: string;
  label: string;
  is_signed: boolean;
  signer_name?: string | null | undefined;
  date?: string | null | undefined;
};

/** @internal */
export const Signature$outboundSchema: z.ZodType<
  Signature$Outbound,
  z.ZodTypeDef,
  Signature
> = z.object({
  type: z.literal("Signature").default("Signature" as const),
  content: z.string(),
  description: z.string(),
  label: z.string(),
  isSigned: z.boolean(),
  signerName: z.nullable(z.string()).optional(),
  date: z.nullable(z.string()).optional(),
}).transform((v) => {
  return remap$(v, {
    isSigned: "is_signed",
    signerName: "signer_name",
  });
});

export function signatureToJSON(signature: Signature): string {
  return JSON.stringify(Signature$outboundSchema.parse(signature));
}
export function signatureFromJSON(
  jsonString: string,
): SafeParseResult<Signature, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Signature$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Signature' from JSON`,
  );
}
