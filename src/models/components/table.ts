/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type Table = {
  type?: "Table" | undefined;
  /**
   * The Table in HTML.
   */
  content: string;
  /**
   * A brief summary of the content in the table.
   */
  description: string;
  /**
   * Optional normalized header rows range used for <thead> construction.
   */
  headerRange?: string | null | undefined;
};

/** @internal */
export const Table$inboundSchema: z.ZodType<Table, z.ZodTypeDef, unknown> = z
  .object({
    type: z.literal("Table").default("Table"),
    content: z.string(),
    description: z.string(),
    header_range: z.nullable(z.string()).optional(),
  }).transform((v) => {
    return remap$(v, {
      "header_range": "headerRange",
    });
  });
/** @internal */
export type Table$Outbound = {
  type: "Table";
  content: string;
  description: string;
  header_range?: string | null | undefined;
};

/** @internal */
export const Table$outboundSchema: z.ZodType<
  Table$Outbound,
  z.ZodTypeDef,
  Table
> = z.object({
  type: z.literal("Table").default("Table" as const),
  content: z.string(),
  description: z.string(),
  headerRange: z.nullable(z.string()).optional(),
}).transform((v) => {
  return remap$(v, {
    headerRange: "header_range",
  });
});

export function tableToJSON(table: Table): string {
  return JSON.stringify(Table$outboundSchema.parse(table));
}
export function tableFromJSON(
  jsonString: string,
): SafeParseResult<Table, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Table$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Table' from JSON`,
  );
}
