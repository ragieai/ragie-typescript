/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Location within linear text using zero-based character offsets.
 */
export type CharacterIndexLocation = {
  locationType: "character_index";
  /**
   * Start character index (inclusive)
   */
  startCharIndex: number;
  /**
   * End character index (exclusive)
   */
  endCharIndex: number;
};

/** @internal */
export const CharacterIndexLocation$inboundSchema: z.ZodType<
  CharacterIndexLocation,
  z.ZodTypeDef,
  unknown
> = z.object({
  location_type: z.literal("character_index"),
  start_char_index: z.number().int(),
  end_char_index: z.number().int(),
}).transform((v) => {
  return remap$(v, {
    "location_type": "locationType",
    "start_char_index": "startCharIndex",
    "end_char_index": "endCharIndex",
  });
});
/** @internal */
export type CharacterIndexLocation$Outbound = {
  location_type: "character_index";
  start_char_index: number;
  end_char_index: number;
};

/** @internal */
export const CharacterIndexLocation$outboundSchema: z.ZodType<
  CharacterIndexLocation$Outbound,
  z.ZodTypeDef,
  CharacterIndexLocation
> = z.object({
  locationType: z.literal("character_index"),
  startCharIndex: z.number().int(),
  endCharIndex: z.number().int(),
}).transform((v) => {
  return remap$(v, {
    locationType: "location_type",
    startCharIndex: "start_char_index",
    endCharIndex: "end_char_index",
  });
});

export function characterIndexLocationToJSON(
  characterIndexLocation: CharacterIndexLocation,
): string {
  return JSON.stringify(
    CharacterIndexLocation$outboundSchema.parse(characterIndexLocation),
  );
}
export function characterIndexLocationFromJSON(
  jsonString: string,
): SafeParseResult<CharacterIndexLocation, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CharacterIndexLocation$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CharacterIndexLocation' from JSON`,
  );
}
