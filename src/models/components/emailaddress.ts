/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type EmailAddress = {
  type?: "EmailAddress" | undefined;
  /**
   * The text content
   */
  content: string;
};

/** @internal */
export const EmailAddress$inboundSchema: z.ZodType<
  EmailAddress,
  z.ZodTypeDef,
  unknown
> = z.object({
  type: z.literal("EmailAddress").default("EmailAddress"),
  content: z.string(),
});
/** @internal */
export type EmailAddress$Outbound = {
  type: "EmailAddress";
  content: string;
};

/** @internal */
export const EmailAddress$outboundSchema: z.ZodType<
  EmailAddress$Outbound,
  z.ZodTypeDef,
  EmailAddress
> = z.object({
  type: z.literal("EmailAddress").default("EmailAddress" as const),
  content: z.string(),
});

export function emailAddressToJSON(emailAddress: EmailAddress): string {
  return JSON.stringify(EmailAddress$outboundSchema.parse(emailAddress));
}
export function emailAddressFromJSON(
  jsonString: string,
): SafeParseResult<EmailAddress, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EmailAddress$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EmailAddress' from JSON`,
  );
}
