/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type Figure = {
  type?: "Figure" | undefined;
  /**
   * The text visible inside the visual (OCR)
   */
  content: string;
  /**
   * A detailed description of what the visual depicts.
   */
  description: string;
  base64Data?: string | null | undefined;
};

/** @internal */
export const Figure$inboundSchema: z.ZodType<Figure, z.ZodTypeDef, unknown> = z
  .object({
    type: z.literal("Figure").default("Figure"),
    content: z.string(),
    description: z.string(),
    base64_data: z.nullable(z.string()).optional(),
  }).transform((v) => {
    return remap$(v, {
      "base64_data": "base64Data",
    });
  });
/** @internal */
export type Figure$Outbound = {
  type: "Figure";
  content: string;
  description: string;
  base64_data?: string | null | undefined;
};

/** @internal */
export const Figure$outboundSchema: z.ZodType<
  Figure$Outbound,
  z.ZodTypeDef,
  Figure
> = z.object({
  type: z.literal("Figure").default("Figure" as const),
  content: z.string(),
  description: z.string(),
  base64Data: z.nullable(z.string()).optional(),
}).transform((v) => {
  return remap$(v, {
    base64Data: "base64_data",
  });
});

export function figureToJSON(figure: Figure): string {
  return JSON.stringify(Figure$outboundSchema.parse(figure));
}
export function figureFromJSON(
  jsonString: string,
): SafeParseResult<Figure, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Figure$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Figure' from JSON`,
  );
}
