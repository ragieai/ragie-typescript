/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type Formula = {
  type?: "Formula" | undefined;
  /**
   * The formula as plain text.
   */
  content: string;
  /**
   * The LaTeX representation of the formula.
   */
  latex?: string | null | undefined;
};

/** @internal */
export const Formula$inboundSchema: z.ZodType<Formula, z.ZodTypeDef, unknown> =
  z.object({
    type: z.literal("Formula").default("Formula"),
    content: z.string(),
    latex: z.nullable(z.string()).optional(),
  });
/** @internal */
export type Formula$Outbound = {
  type: "Formula";
  content: string;
  latex?: string | null | undefined;
};

/** @internal */
export const Formula$outboundSchema: z.ZodType<
  Formula$Outbound,
  z.ZodTypeDef,
  Formula
> = z.object({
  type: z.literal("Formula").default("Formula" as const),
  content: z.string(),
  latex: z.nullable(z.string()).optional(),
});

export function formulaToJSON(formula: Formula): string {
  return JSON.stringify(Formula$outboundSchema.parse(formula));
}
export function formulaFromJSON(
  jsonString: string,
): SafeParseResult<Formula, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Formula$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Formula' from JSON`,
  );
}
