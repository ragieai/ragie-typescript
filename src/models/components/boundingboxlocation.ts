/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Location within a paginated document using normalized coordinates.
 */
export type BoundingBoxLocation = {
  locationType: "bounding_box";
  /**
   * Left coordinate (0.0 to 1.0)
   */
  left: number;
  /**
   * Top coordinate (0.0 to 1.0)
   */
  top: number;
  /**
   * Width of the element (0.0 to 1.0)
   */
  width: number;
  /**
   * Height of the element (0.0 to 1.0)
   */
  height: number;
  /**
   * Page number
   */
  pageNumber: number;
};

/** @internal */
export const BoundingBoxLocation$inboundSchema: z.ZodType<
  BoundingBoxLocation,
  z.ZodTypeDef,
  unknown
> = z.object({
  location_type: z.literal("bounding_box"),
  left: z.number(),
  top: z.number(),
  width: z.number(),
  height: z.number(),
  page_number: z.number().int(),
}).transform((v) => {
  return remap$(v, {
    "location_type": "locationType",
    "page_number": "pageNumber",
  });
});
/** @internal */
export type BoundingBoxLocation$Outbound = {
  location_type: "bounding_box";
  left: number;
  top: number;
  width: number;
  height: number;
  page_number: number;
};

/** @internal */
export const BoundingBoxLocation$outboundSchema: z.ZodType<
  BoundingBoxLocation$Outbound,
  z.ZodTypeDef,
  BoundingBoxLocation
> = z.object({
  locationType: z.literal("bounding_box"),
  left: z.number(),
  top: z.number(),
  width: z.number(),
  height: z.number(),
  pageNumber: z.number().int(),
}).transform((v) => {
  return remap$(v, {
    locationType: "location_type",
    pageNumber: "page_number",
  });
});

export function boundingBoxLocationToJSON(
  boundingBoxLocation: BoundingBoxLocation,
): string {
  return JSON.stringify(
    BoundingBoxLocation$outboundSchema.parse(boundingBoxLocation),
  );
}
export function boundingBoxLocationFromJSON(
  jsonString: string,
): SafeParseResult<BoundingBoxLocation, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BoundingBoxLocation$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BoundingBoxLocation' from JSON`,
  );
}
