/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type SubHeader = {
  type?: "SubHeader" | undefined;
  /**
   * The text content
   */
  content: string;
};

/** @internal */
export const SubHeader$inboundSchema: z.ZodType<
  SubHeader,
  z.ZodTypeDef,
  unknown
> = z.object({
  type: z.literal("SubHeader").default("SubHeader"),
  content: z.string(),
});
/** @internal */
export type SubHeader$Outbound = {
  type: "SubHeader";
  content: string;
};

/** @internal */
export const SubHeader$outboundSchema: z.ZodType<
  SubHeader$Outbound,
  z.ZodTypeDef,
  SubHeader
> = z.object({
  type: z.literal("SubHeader").default("SubHeader" as const),
  content: z.string(),
});

export function subHeaderToJSON(subHeader: SubHeader): string {
  return JSON.stringify(SubHeader$outboundSchema.parse(subHeader));
}
export function subHeaderFromJSON(
  jsonString: string,
): SafeParseResult<SubHeader, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SubHeader$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SubHeader' from JSON`,
  );
}
