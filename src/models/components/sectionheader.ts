/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type SectionHeader = {
  type?: "SectionHeader" | undefined;
  /**
   * The text content
   */
  content: string;
};

/** @internal */
export const SectionHeader$inboundSchema: z.ZodType<
  SectionHeader,
  z.ZodTypeDef,
  unknown
> = z.object({
  type: z.literal("SectionHeader").default("SectionHeader"),
  content: z.string(),
});
/** @internal */
export type SectionHeader$Outbound = {
  type: "SectionHeader";
  content: string;
};

/** @internal */
export const SectionHeader$outboundSchema: z.ZodType<
  SectionHeader$Outbound,
  z.ZodTypeDef,
  SectionHeader
> = z.object({
  type: z.literal("SectionHeader").default("SectionHeader" as const),
  content: z.string(),
});

export function sectionHeaderToJSON(sectionHeader: SectionHeader): string {
  return JSON.stringify(SectionHeader$outboundSchema.parse(sectionHeader));
}
export function sectionHeaderFromJSON(
  jsonString: string,
): SafeParseResult<SectionHeader, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SectionHeader$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SectionHeader' from JSON`,
  );
}
