/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type Stamp = {
  type?: "Stamp" | undefined;
  /**
   * The text inside the stamp
   */
  content?: string | undefined;
  /**
   * A detailed description of what the visual depicts.
   */
  description?: string | undefined;
};

/** @internal */
export const Stamp$inboundSchema: z.ZodType<Stamp, z.ZodTypeDef, unknown> = z
  .object({
    type: z.literal("Stamp").default("Stamp"),
    content: z.string().default(""),
    description: z.string().default(""),
  });
/** @internal */
export type Stamp$Outbound = {
  type: "Stamp";
  content: string;
  description: string;
};

/** @internal */
export const Stamp$outboundSchema: z.ZodType<
  Stamp$Outbound,
  z.ZodTypeDef,
  Stamp
> = z.object({
  type: z.literal("Stamp").default("Stamp" as const),
  content: z.string().default(""),
  description: z.string().default(""),
});

export function stampToJSON(stamp: Stamp): string {
  return JSON.stringify(Stamp$outboundSchema.parse(stamp));
}
export function stampFromJSON(
  jsonString: string,
): SafeParseResult<Stamp, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Stamp$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Stamp' from JSON`,
  );
}
