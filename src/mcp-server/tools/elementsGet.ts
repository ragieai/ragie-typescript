/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { elementsGet } from "../../funcs/elementsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetElementRequest$inboundSchema,
};

export const tool$elementsGet: ToolDefinition<typeof args> = {
  name: "elements-get",
  description: `Get Element For Document

Returns an element for a specific id`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await elementsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
