/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { partitionsEnableMcp } from "../../funcs/partitionsEnableMcp.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.EnableMcpPartitionsPartitionIdMcpPostRequest$inboundSchema,
};

export const tool$partitionsEnableMcp: ToolDefinition<typeof args> = {
  name: "partitions-enable-mcp",
  description: `Enable Mcp

Enables context-aware descriptions for a partition. This will allow the automatically generate a desccription for based on the documents in the partition.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await partitionsEnableMcp(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
